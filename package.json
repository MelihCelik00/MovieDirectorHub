{
  "name": "movie-director-hub",
  "version": "1.0.0",
  "description": "A microservices-based backend solution for managing movies and directors",
  "private": true,
  "workspaces": [
    "shared",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "lint:fix": "npm run lint:fix --workspaces"
  }
} 